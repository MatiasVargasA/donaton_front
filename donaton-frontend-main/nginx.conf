server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        # Esta línea es la magia que hace que React Router funcione al recargar
        try_files $uri $uri/ /index.html;
    }
}